server:
  port: 8888

logging:
  level:
    ROOT: INFO
    org.spring.framework.cloud.config: DEBUG

spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: file://Users/simon/Documents/project/java/microservices/microservices-demo/config-server-repository
          clone-on-start: true
    fail-fast: true